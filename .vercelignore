# Files kept in the repo but never published.
#
# Vercel serves the repo root as static files, so anything committed here is
# reachable on the live domain. These were all fetchable at 200 until now —
# superseded drafts of the site competing with index.html for search results.

# Earlier versions of the site, kept for history.
SHAMUM Website.html
index.html.bak
shamum-website.html

# Project tooling, not web content.
website/

# Session notes. Reachable at 200 on the live domain until now: it names the
# deploy identity, the repo, and open questions about the products.
HANDOFF.md

# Local Blender/Cycles pipeline and its frame output.
render/
assets/marwaframes/

# Local preview of the reduced-motion build (render/preview_reduced.py).
_rmtest.html

# Agent skills installed by `npx skills add` (Leonxlnx/taste-skill, 2026-09-03).
# Editor tooling, not web content. Untracked today, but they sit at the repo
# root, so the moment anyone commits them they would be fetchable at 200 on the
# live domain — the exact leak the top of this file is about.
.agents/
.claude/skills/
skills-lock.json
